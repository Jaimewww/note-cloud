<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 5.3//EN"
        "http://hibernate.org/dtd/hibernate-configuration-5.3.dtd">

<hibernate-configuration>
    <session-factory>

        <!-- Configuración de la conexión a la base de datos -->
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="hibernate.connection.url">jdbc:mysql://localhost:3306/notecloud?useSSL=false&amp;serverTimezone=UTC</property>
        <property name="hibernate.connection.username">root</property>
        <property name="hibernate.connection.password"></property>

        <!-- Dialecto SQL para la base de datos -->
        <property name="hibernate.dialect">org.hibernate.dialect.MySQL8Dialect</property>

        <!-- Mostrar las consultas SQL en la consola -->
        <property name="hibernate.show_sql">true</property>

        <!-- Formatear las consultas SQL en la salida -->
        <property name="hibernate.format_sql">true</property>

        <!-- Actualizar el esquema automáticamente -->
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- Declarar tus clases anotadas -->
        <mapping class="edu.cc.notecloud.persistance.User"/>
        <!-- Agrega más mappings aquí si tienes otras entidades -->

    </session-factory>
</hibernate-configuration>
